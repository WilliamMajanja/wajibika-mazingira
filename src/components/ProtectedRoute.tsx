// This component is obsolete and no longer used.
// The authentication flow is now handled by the `AppContent` component in `App.tsx`
// which checks the `isAuthenticated` state from the `useAuth` hook.
// This file can be safely deleted from the project.
